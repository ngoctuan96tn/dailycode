server:
  port: 9296

spring:
  application:
    name: CONFIG_SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ngoctuan96tn/configserver
          clone-on-start: true
          default-label: main